info:
  name: createitem
  type: http
  seq: 7

http:
  method: POST
  url: "{{base_url}}/items"
  body:
    type: json
    data: |-
      {
        "title": "ddddd",
        "description": "krutoe ochen",
        "price": 8000000,
        "imageUrl": "https://example.com/govno.jpg"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
