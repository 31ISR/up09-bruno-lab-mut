info:
  name: createbids
  type: http
  seq: 10

http:
  method: POST
  url: "{{base_url}}/items/:id/bids"
  params:
    - name: id
      value: "1769028676229"
      type: path
  body:
    type: json
    data: |
      {
        "amount": 850000000000
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
